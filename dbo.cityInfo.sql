CREATE TABLE [dbo].[cityInfo] (
    [Airport name] INT          NOT NULL,
    [City name]    VARCHAR (50) NULL,
    [State]        VARCHAR (50) NULL,
    PRIMARY KEY CLUSTERED ([Airport name] ASC)
);

